# product-analyzer: ignore large/ephemeral paths only (reports/ itself is tracked)
reports/**/downloads/
reports/**/.sandbox_ctl_last_shell.json

# cua-driver bundle (installed locally via install_cua_driver.py)
cua/

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# editors
.vscode/
.idea/
